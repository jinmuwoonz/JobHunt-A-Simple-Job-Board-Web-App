# Example .env.example — copy to .env and fill in real values (do NOT commit .env with secrets)

# Application / Flask
FLASK_APP=run.py
FLASK_ENV=development
DEBUG=True

# Secrets (required in production)
SECRET_KEY=your-secret-key-here

# Database
DATABASE_URL=mysql+pymysql://username:password@localhost/jobhunt_db

# Optional overrides (config.py currently uses defaults)
# UPLOAD_FOLDER_RESUME=app/static/uploads/resumes
# UPLOAD_FOLDER_COVER_LETTER=app/static/uploads/cover_letter

# Notes:
# - Replace placeholders with real values.
# - Add .env to .gitignore before committing.
# - Rotate any secrets already committed to the repo.